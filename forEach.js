//foreach()=> se podria traducir por cada elemento. Por cada elemento muestrame o traeme el elemento o su indice.

const animales = ['Puma', 'Tigre', 'Pantera', 'Leon', 'Leopardo', 'Cobra', 'boa', 'mamba'];

animales.forEach((elem, index) => console.log(`${index}: ${elem}`));
